type SmokeWidgetProps = Record<string, never>;

export const SmokeWidget = (_props: SmokeWidgetProps) => {
  return (
    <div className="rounded-lg border border-border/70 bg-card p-4">
      SmokeWidget component scaffolded.
    </div>
  );
};
